spring:
  application:
    name : roc-service-testconfig
  profiles:
      active: dev,pre
  cloud:
    config:
      uri: http://localhost:8888


# 关闭安全策略
management :
  security :
      enabled : false